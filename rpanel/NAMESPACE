export(
# widget functions
# rp,
# rp.assign,  
  rp.block, 
  rp.button,
  rp.checkbox,
# rp.checkbox.change,
  rp.clearlines,
  rp.combo,
  rp.control,
  rp.control.dispose,
  rp.deleteline,
  rp.do, 
  rp.doublebutton,
# rp.env,
  rp.grid,
  rp.image,
  rp.line,
  rp.listbox,
  rp.menu,
  rp.messagebox,
  rp.notebook,
  rp.notebook.raise,
  rp.onesample,
  rp.pos,
  rp.radiogroup,
  rp.screenresolution,
# rp.settings,
# rp.setup,
  rp.slider,
  rp.slider.change,
  rp.text,
  rp.text.change,
  rp.textentry, 
  rp.timer,
  rp.tkrplot,
  rp.tkrreplot,
  rp.twosample,
  rp.widget.dispose,
  rp.var.get,
  rp.var.put,
# rp.control.get,
  rp.control.put,
  rp.panel, # hangover from old version, just a wrapper for rp.control.get

# cartoon functions
  rp.ancova,
  rp.anova,
  rp.bubbleplot,
  rp.cartoons,
  rp.ci, 
  rp.coefficients,
  rp.colour.key,
  rp.datalink,
  rp.firth,
  rp.geosim, 
  rp.gulls, 
  rp.likelihood,
  rp.lmsmall,
  rp.logistic,
# rp.multilevel,
  rp.mururoa, 
  rp.normal, 
  rp.onesample,
  rp.plot3d, 
  rp.plot4d,
  rp.power,
  rp.sample,
  rp.spacetime,
  rp.surface,
  rp.regression, 
  rp.regression2, 
  rp.rmplot, 
  rp.tables,
  rp.wrangle
# setSliderRatePolicy
)

import(tcltk)
importFrom("grDevices", "col2rgb", "grey", "heat.colors", "rgb2hsv",
           "terrain.colors", "topo.colors")
importFrom("graphics", "abline", "axTicks", "axis", "box", "hist",
           "image", "layout", "lines", "mtext", "par", "persp", "plot",
           "points", "polygon", "rect", "segments", "strheight",
           "text", "title")
importFrom("stats", "as.formula", "binomial", "coef", "dnorm",
           "fitted", "glm", "lm", "na.exclude", "optim", "pnorm",
           "qqnorm", "qt", "rnorm", "runif", "sd", "pf")
importFrom("utils", "demo", "menu")
# importFrom("shiny", "runApp", "shinyApp", "reactiveValues",
#            "observeEvent", "renderPlot", "reactive")

# Options for the future
# rp.table, rp.table.value.get, rp.table.value.set, rp.table.location, rp.table.get,
# rp.hide, rp.disable, rp.enable,
# rp. scrollbars on window?
# rp. dialogs - but these are windows only?
# rp. Check list of widgets for further widgets
